lista de correcções

dava um errp porque o programa tentava escrever um campo na bd como longInt a chave estrangeira não permitia

public class Pokemon {

    @Id
    @Column(name = "id_pokemon")
    private Long idPokemon;
	
	para
	
	public class Pokemon {

    @Id
    @Column(name = "id_pokemon")
    private Integer idPokemon;
	
Criei a página para abilitar os pokemons que desabilitamos

alterei a página index nomeadamente o h1

corregi vários registos na bd, export na pasta _bdexport

a página do edit types fuinciona, mas tem de ser melhorada, o botao para lá chegar ainda não funciona

http://localhost:8080/pokemon/1/edit/types

as listagens de pokemons ficam com o topo das colunas fixo, não consegui que o h1 no topo da página tambem ficasse

melhorei o about us, (espero!)